@model IEnumerable<CourseApp.Models.Contact>
@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    <p>
        @item.Email <br /><hr />
        @item.Phone
    </p>
}
